export interface Roll {
  roll_name: string;
  default_roll: string;
}

export interface AbilityScore {
  ability: string;
  score: number;
}

export interface CharacterInfo {
  info_type: string;
  input: string;
}
